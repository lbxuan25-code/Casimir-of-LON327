# BdG Total Kernel 输出

- `data/`：$K_{\mathrm{total}}(i\xi) = K_{\mathrm{para}}(i\xi) + K_{\mathrm{dia}}$
  扫描的 `.npz` 数据。
- `figures/`：$K_{\mathrm{total}}(i\xi)$ 的 $C_4$ 对称性和分量诊断图。

这些输出是虚频轴 BdG electromagnetic kernel 诊断；它们目前不是 Casimir 输入，
也不标记为实验 optical conductivity。
