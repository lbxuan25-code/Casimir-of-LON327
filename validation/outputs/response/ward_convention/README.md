# Ward convention validation

本目录保存 response / Ward convention 的轻量验证证据。

核心摘要见 `ward_convention_summary.md`。复现入口见 `command.sh`。

旧的 stage4 JSON/MD 和各类 compact/raw data 已归并为 summary，不再作为长期 validation artifact 保存。
