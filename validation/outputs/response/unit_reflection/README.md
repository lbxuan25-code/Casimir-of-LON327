# Unit conversion and reflection input validation

本目录保存 unit conversion、sheet conductivity、reflection input 和 TE/TM adapter 的 consolidated evidence。

长期保留文件：

- `unit_reflection_summary.md`：中文摘要和边界。
- `unit_reflection_status.json`：机器可读状态 marker。
- `command.sh`：主要复现入口。

旧的 stage5 JSON/MD 已归并到 summary/status。validation scripts 保留，可按需重新生成原始输出。
