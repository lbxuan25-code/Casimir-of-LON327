# Normal-State Scripts

This folder collects scripts that use only the adopted normal-state
Hamiltonian and normal-state Kubo conductivity baseline.

Compatibility wrappers remain at `scripts/*.py`, so older commands still work.
